export function add(x=1,y=2)
{
    return x+y;
}

export function userName(username,...userfriends)
{
    console.log(userName+" "+userfriends);
}

export function printCapitalNames(name1,name2,name3,name4,name4)
{
    console.log(name1.toUpperCase()+name2.toUpperCase()+name3.toUpperCase()+name4.toUpperCase()+name5.toUpperCase());
}
